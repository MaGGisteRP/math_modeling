print ('Введите элементы массива:')
import numpy as np 
A = np.zeros([])
a = len(A)

def ladno(a):
    s = sum(a)
    return s/a
for a in range:
    
    
    
def ladno(): 
   numb = np.zeros(1)
   nu
    while True: